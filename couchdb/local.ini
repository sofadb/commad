[httpd]
enable_cors = true

[cors]
origins = https://sofadb.github.io/
credentials = true
methods = GET, PUT, POST, HEAD, DELETE, OPTIONS
headers = accept, authorization, content-type, origin, referer, x-csrf-token, if-match, destination

[chttpd]
enable_cors = true
